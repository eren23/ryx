# Task: Add collectible item system to raycaster

Add collectible items to `examples/raycaster.ryx`.

Changes:
1. Add `let mut items = [...]` array matching the 16x16 world map. Place 1 values at 6-8 open floor positions (where world_map has 0). Initialize all other positions to 0.
2. Add `let mut score = 0` and `let mut total_items = <count>` as mutable captured variables.
3. In the frame callback, after movement logic: check `items[floor(pos_y) * 16 + floor(pos_x)]`. If == 1, set to 0 and increment `score`.
4. On minimap rendering: add a pass that draws small colored dots (cyan, e.g., `gfx_set_color(gfx_rgb(0, 255, 255))`) for positions where `items[i] == 1`.

Read `examples/raycaster.ryx` for current state, world map layout, and minimap rendering code.

Acceptance: compiles and type-checks. Items are rendered on minimap, walking over them collects them.

Target files: examples/raycaster.ryx

## Testing
Verify: go test ./...

## Context from Previous Runs
No relevant learnings found for this project.