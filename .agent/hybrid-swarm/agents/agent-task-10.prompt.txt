# Task: Tricolor incremental garbage collector with write barriers

Implement pkg/gc/gc.go: Heap struct (Objects slice, FreeList, BytesAlloc, adaptive Threshold starting 1MB growing 2x, GCStats). Tricolor mark-sweep algorithm: root scanning (fiber stacks, call frames, globals, open upvalue cells → mark Gray), tracing (Gray→Black, references→Gray, incremental up to 256 objects per VM slice), sweeping (White objects freed, Black reset to White, incremental up to 512 per slice). GC triggers on BytesAlloc > Threshold, manual __gc(), emergency on allocation failure. Implement pkg/gc/write_barrier.go: if Black parent gets White child reference, re-mark parent Gray. Compiler inserts at STORE_UPVALUE, FIELD_SET, INDEX_SET, channel send. Integrate GC with VM dispatch loop (incremental work between instruction slices). Write gc_test.go: unreachable collected, reachable survive, circular references collected, threshold adaptation, incremental tracing spread, write barrier correctness, stress test 100K objects, concurrent fibers + GC safety, open/closed upvalue lifecycle, channel buffer values not collected, GC stats accuracy.

Target files: pkg/gc/gc.go, pkg/gc/write_barrier.go, pkg/gc/gc_test.go