# Task: Add initial level load and frame callback skeleton

Append to `examples/dungeon_crawler.ryx` Section 7 (initial level load) and Section 8 (frame callback skeleton). Section 7: copy level 1 tile data into tiles array using while loop, populate enemy arrays from get_level_enemies() with +0.5 offset for centering, populate item arrays from get_level_items(), set player start position, initialize boss state, clear particles. Section 8: define `let frame = || { ... };` closure with delta-time computation (default to 1/60 if <= 0), global ESC handling (toggle playing/paused, quit from other states), and state dispatch if/else chain covering all 6 states (STATE_TITLE, STATE_PLAYING, STATE_GAME_OVER, STATE_LEVEL_COMPLETE, STATE_VICTORY, STATE_PAUSED) with `()` placeholder expressions. Close with `gfx_run(frame)` and `}` for main. Reference exact code from goal sections T6 and T7. The placeholder expressions will be replaced by subsequent tasks.

Target files: examples/dungeon_crawler.ryx

## Testing
Verify: go test ./...

## Context from Previous Runs
No relevant learnings found for this project.