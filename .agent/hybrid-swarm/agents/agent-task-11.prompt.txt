# Task: Add compile-only integration test for dungeon_crawler

Modify `tests/integration/e2e_test.go` to add a `TestDungeonCrawlerCompiles` function. Read the existing test file first to understand the test patterns and available helper functions (like compileAndRun). The test should: read `../../examples/dungeon_crawler.ryx` with os.ReadFile, skip if not found, call the compile helper, and verify no compilation errors occur. Since the program calls gfx_init/gfx_run which requires a display, runtime errors related to graphics/display/ebiten should be tolerated — only fail on actual compile errors. Make sure to add any needed imports (e.g., `strings` for strings.Contains). Reference the exact test code from goal section T18.

Target files: tests/integration/e2e_test.go

## Related Tests
These test files cover your target files — run them to verify:
- tests/integration/e2e_test.go

Verify: go test ./... tests/integration/e2e_test.go

## Context from Previous Runs
No relevant learnings found for this project.