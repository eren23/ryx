# Task: Implement player input and movement

Inside the STATE_PLAYING branch of the frame callback, replace the gameplay placeholder with player movement code. Implementation: if player_moving is true, increment player_move_progress by dt/PLAYER_MOVE_TIME(), snap to target when >= 1.0, else lerp between start and target. If not moving, check WASD/arrow keys (W/Up first, then S/Down, A/Left, D/Right), compute target tile, check is_walkable(), start movement if valid (set start pos, target, moving=true, progress=0.0, facing direction). After movement block: decrement player_attack_timer, player_damage_flash, and player_invuln_timer by dt when > 0. Use `gfx_key_pressed()` for held keys, `float_to_int(floor(player_x))` for current tile. Reference exact code from goal section T10.

Target files: examples/dungeon_crawler.ryx

## Testing
Verify: go test ./...

## Context from Previous Runs
No relevant learnings found for this project.