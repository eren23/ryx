# Task: Verify complete build and test suite

Final verification task. Run: (1) `go build ./cmd/ryx` to ensure the compiler builds. (2) `wc -l examples/dungeon_crawler.ryx` to verify the file is approximately 1400-1600 lines. (3) `go test ./tests/integration/ -run DungeonCrawler -v` to verify the new test passes. (4) `go test ./... -short` to verify all existing tests still pass. Check that: the .ryx file has all 18 sections from T1-T17 present, array sizes are correct (tiles=300, enemies=32, particles=64, items=16), all level tile arrays have exactly 300 elements, variable names are consistent throughout the file, and the file structure matches the documented layout (helper functions before main, state inside main, frame callback closure, gfx_run call at end).

## Testing
Verify: go test ./...

## Context from Previous Runs
No relevant learnings found for this project.