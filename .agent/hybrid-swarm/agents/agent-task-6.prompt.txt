# Task: Implement enemy AI and movement

Add enemy AI code inside STATE_PLAYING after player movement. Loop through enemies (while eai < enemy_count), skip dead ones. For each alive enemy: compute distance to player. If within detect range: chase mode — compute normalized direction vector, move toward player (or away if skeleton with hp < 15). Ghosts ignore walls (move freely), others check walkability per-axis independently. If outside range: patrol mode — decrement enemy_timer, pick random direction when timer <= 0, move at 50% speed, bounce off walls by picking new direction. Boss special: when hp < 75 and !boss_spawned_minions, spawn 2 slimes at adjacent positions if enemy_count < MAX_ENEMIES()-1, increment enemy_count by 2. All movement multiplied by dt for frame independence. Reference exact code from goal section T11.

Target files: examples/dungeon_crawler.ryx

## Testing
Verify: go test ./...

## Context from Previous Runs
No relevant learnings found for this project.