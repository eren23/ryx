# Task: Implement menu screens (title, game over, victory, pause)

Replace the placeholder `()` expressions in the frame callback's STATE_TITLE, STATE_GAME_OVER, STATE_VICTORY, and STATE_PAUSED branches with full implementations. STATE_TITLE: black background, gold title text 'RYX DUNGEON CRAWLER', subtitle, controls info, 'Press ENTER to start', transition to STATE_PLAYING on ENTER. STATE_GAME_OVER: black bg, red 'GAME OVER', show level and total_score+score, on ENTER do full game reset (reset all state vars, reload level 1 data into all arrays using same pattern as Section 7, transition to STATE_TITLE). STATE_VICTORY: black bg, gold 'VICTORY!', show final score, ENTER returns to title. STATE_PAUSED: semi-transparent black overlay, 'PAUSED' text, ESC resumes (handled by global ESC), ENTER calls gfx_quit(). Use `++` for string concatenation, `int_to_string()` for numbers. Reference exact code from goal section T9.

Target files: examples/dungeon_crawler.ryx

## Testing
Verify: go test ./...

## Context from Previous Runs
No relevant learnings found for this project.