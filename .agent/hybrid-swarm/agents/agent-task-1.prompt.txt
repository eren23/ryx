# Task: Create dungeon_crawler.ryx with all pre-main helper functions

Create the file `examples/dungeon_crawler.ryx` and write Sections 1-5: Color Utilities (color_r, color_g, color_b, color_a, color_scale, color_lerp, color_darken, color_lighten), Game Constants (screen_w, screen_h, tile_size, map_cols/rows/len, all TILE_* constants, MAX_* limits, PLAYER_* constants, ENEMY_* type constants, ITEM_* type constants, STATE_* constants), Map & Tile Helpers (tile_at, is_walkable, tile_to_px/py, px_to_tile/py_to_tile, dist_sq, dist, manhattan, tile_color, floor_color, enemy_color/size/detect_range/speed/max_hp/damage, item_color), Level Data (3 level tile arrays of 300 elements each, enemy/item spawn data, start positions, dispatcher functions get_level_tiles/enemies/items/start_x/start_y, total_levels), and Particle Velocity Helpers (burst_vx/vy, spiral_vx/vy, attack_vx/vy). These are all pure functions defined before fn main(). Reference the exact code blocks from goal sections T1-T4. Read `examples/raycaster.ryx` for Ryx syntax patterns. Key Ryx syntax: no negative literals (use `0.0 - value`), hex literals like `0x1000000`, zero-arg builtins need parens (`pi()`, `COLOR_RED()`), string concat is `++`.

Target files: examples/dungeon_crawler.ryx

## Testing
Verify: go test ./...

## Context from Previous Runs
No relevant learnings found for this project.