# Task: Add fn main() with all mutable state declarations

Append to `examples/dungeon_crawler.ryx` the opening of `fn main()` containing Section 6: all mutable game state variable declarations. This includes: game state machine vars (game_state, current_level, score, total_score, level_transition_timer), player state vars (player_x/y as Float, player_target_x/y as Int, player_moving, player_move_progress, player_start_x/y, player_hp, player_facing, player_attack_timer, player_damage_flash, player_invuln_timer), map tiles array (300 zeros), enemy parallel arrays (enemy_x/y as 32 floats, enemy_type/hp/alive as 32 ints, enemy_dir/timer as 32 floats, enemy_count), particle parallel arrays (part_x/y/vx/vy/life as 64 floats, part_color/part_active as 64 ints), item parallel arrays (item_x/y/type/active as 16 ints, item_count), boss state (boss_spawned_minions, boss_alive, exit_unlocked), wall texture handles (wall_img, wall2_img via gfx_load_image), and gfx_init call. Every variable name here is referenced by all subsequent tasks — names must match exactly. Reference the exact code from goal section T5.

Target files: examples/dungeon_crawler.ryx

## Testing
Verify: go test ./...

## Context from Previous Runs
No relevant learnings found for this project.